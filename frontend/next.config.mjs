/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow longer serverless function execution for the webhook proxy
  // Vercel Hobby: 60s max, Pro: up to 300s
}

export default nextConfig
